<!--pages/Home/Home.wxml-->
<view>{{message}}</view>
<view wx:for="{{students}}" wx:for-index="{{idx}}">name: {{item.name}} - age: {{item.age}}</view>
<view>{{counter}}</view>
<button bindtap="addClick">+</button>

<!-- tag 1 -->